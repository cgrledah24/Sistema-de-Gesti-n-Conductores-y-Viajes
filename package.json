{
  "name": "driver-management-netlify",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "vite": "5.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.x.x"
  }
}
